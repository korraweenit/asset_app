gemini "ทำการ Refactor โค้ดของโปรเจกต์นี้โดย:
1. ย้ายฟังก์ชัน 'fetch_data' (รวมถึง @st.cache_data) ไปไว้ที่ไฟล์ backend/data.py (อย่าลืม import yfinance, pandas, datetime และ streamlit ไว้ด้านบนของไฟล์นั้นด้วย)
2. ย้ายฟังก์ชัน 'calculate_metrics' และ 'calculate_portfolio_return' ไปไว้ที่ไฟล์ backend/calc.py (อย่าลืม import pandas, numpy)
3. ในไฟล์ app.py ให้ลบฟังก์ชันเหล่านั้นทิ้งไป และเพิ่มคำสั่ง import ฟังก์ชันเหล่านั้นจาก backend.data และ backend.calc เข้ามาใช้งานแทน
4. ตรวจสอบให้แน่ใจว่าลอจิก DCA และ Rebalance เดิมไม่สูญหาย" --mode auto-edit